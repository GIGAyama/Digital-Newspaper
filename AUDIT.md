# デジタル新聞 点検記録（GIGA Standard v4）

2026-08-03 時点。数字はすべて**実測**。測っていないものは「未計測」と書く。

## 測り方

本番（`script.google.com`）へは作業環境から到達できないため、
**GAS が返す画面と同じものを手元で組み立てて**測った。
スクリプトレット（`<?= ScriptApp.getService().getUrl() ?>`）は畳み、
`google.script.run` は差し替えのダミーにしている。

計測環境：Chromium 141（Playwright）、1280×900 / DPR 2。
道具は Digital_textbook の
[`scripts/measure/`](https://github.com/GIGAyama/Digital_textbook/tree/main/scripts/measure)。

**測れていないもの**：送信後の画面、管理画面（`admin.html`）、
サーバーの戻り値に依存する部分。

---

## 1. まとめ

| 区分 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満 | **5件** | **0件** |
| タップ44px未満 | **1件** | **0件** |
| `viewport-fit=cover` | 無し | 有り |
| `env(safe-area-inset-*)` | 無し | 有り |
| `100dvh` | 無し | 有り |
| `prefers-reduced-motion` | 無し | 有り |
| `forced-colors` | 無し | 有り |
| LICENSE / dependabot | 無し | 追加 |

---

## 2. いちばん重い：ふりがなが送信ボタンの上で読めなかった

```html
<button type="submit" id="submitBtn"><ruby>送<rt>そう</rt>信<rt>しん</rt></ruby>する</button>
```

`rt { color: #555 }` と決め打ちしていたため、**色のついた送信ボタンの上で比 1.47**。
ほぼ読めない。

しかも、ふりがなが必要なのは低学年の児童。
**いちばん読めなくて困る人が、いちばん読めない**という形になっていた。

このリポジトリは、タグのラベルについてだけ同じ問題に気づいて手当てしてあった。

```css
.tag-radio:checked + .tag-label rt { color: rgba(255,255,255,0.9); }
```

**気づいていたのに、ボタンのほうは漏れていた。** 1か所ずつ潰すのではなく、
色のついた面ではまとめて色を継がせるようにした。

```css
rt { color: #5f6368; }
button rt, .tag-label rt, .success-title rt, [class*="btn"] rt { color: inherit; }
```

---

## 3. そのほかのコントラスト

| 対象 | 色 | 比 | 直し方 |
|---|---|---:|---|
| ふりがな（ボタン上） | `#555` | **1.47** | 色を継がせる |
| 「ここをタップして写真を選んでね」 | `#888` | 3.36 | `#5f6368` |
| 「編集ページへ」 | `#7f8c8d` | 3.48 | `#5a6673` |
| 「タグ設定がありません」 | `red` | 4.00 | `#c0221f` |

「タグ設定がありません」は、**先生の設定が終わっていないときに出る文言**。
いちばん困っている人に向けた文が、いちばん読みにくい色だった。

---

## 4. タップ領域

「編集ページへ」が 129×41。あと 3px 足りなかった。
`display: inline-flex` ＋ `min-height: 44px` にした。見た目はほぼ変わらない。

---

## 5. 未対応・判断が要る点

- **PWA 化していない**（manifest / Service Worker / offline.html なし）。
  C型は `script.google.com` 配信のため、GitHub Pages 側のシェル（C+型）を
  作らないと PWA にできない。構成の変更なので別途判断が要る。
- **管理画面（`admin.html`）は未計測。** 同じ書き方が使われている可能性が高い。
- CSP は入れていない。GAS は `HtmlService` が独自のサンドボックスで包むため、
  `<meta>` の CSP がどう働くかを本番で確かめないと判断できない。
- 本番の URL・デプロイ状態は未確認（到達できないため）。

---

## 6. この PR をマージしてよいか

**`index.html` は GAS 本体**であり、この作業環境から本番の挙動を確認できません。
表示まわりは手元で実測していますが、**サーバー側の動作確認は取れていません。**

そのため**自動ではマージしていません。** 内容を確認のうえ、
テスト用のデプロイで一度動かしてから取り込んでください。
