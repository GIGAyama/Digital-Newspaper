# デジタル新聞 点検記録（GIGA Standard v5）

2026-08-08 時点。数字はすべて**実測**。測っていないものは「未計測」と書く。

**この記録は、直す前に測った値と、直したあとに測り直した値の両方を載せている。**
「改修前は壊れていた」と書くときは、その版を実際に取り出して測った（v5 §7-6）。

- [測り方](#測り方)
- [1. まとめ](#1-まとめ)
- [2. 監査表 A〜G](#2-監査表-ag)
- [3. コントラスト（改修前 31要素 → 0）](#3-コントラスト改修前-31要素--原因-20箇所--0)
- [4. タップ領域（改修前 73箇所 → 0）](#4-タップ領域改修前-73箇所--0)
- [5. 依存 — 学校で塞がれたときに何が起きるか](#5-依存--学校で塞がれたときに何が起きるか)
- [6. GAS 固有（§5）](#6-gas-固有5)
- [7. 未計測・判断が要る点](#7-未計測判断が要る点)
- [付録. 前回（v4, 2026-08-03）の記録](#付録-前回v4-2026-08-03の記録)

---

## 測り方

本番（`script.google.com`）へは作業環境から到達できないため、
**GAS が返す画面と同じものを手元で組み立てて**測った（v5 §7-3）。

- スクリプトレット（`<?= ScriptApp.getService().getUrl() ?>`）は畳む
- `google.script.run` は、戻り値の見本を返すダミーに差し替える
  （記事4件・タグ5件・保存データ2件。うち2件は画像あり）
- 色は「1px 実際に塗って `getImageData` で読む」方式（v5 §7-2）。
  `oklch()` や `color-mix()` でも壊れない
- グラデーションは停止色をすべて取り、**いちばん比が小さくなる点**で判定する
- 絵文字のみの要素、`disabled` / `cursor: not-allowed` は対象外（WCAG 対象外）
- 見出しラベル（`<label for>` がテキスト入力を指すだけのもの）はタップ対象から除く。
  押す本体は入力欄のほうであるため。**チェックボックス／ラジオを囲むラベルは残す**（§2-9）

計測環境：Chromium 141（Playwright）、1366×768（Chromebook GIGA標準機）と
320×568（設計の下限）、DPR 2。

### CDN の控えを用意してから測った（§7-4）

作業環境から `unpkg.com` / `cdnjs.cloudflare.com` へは出られない。
そのまま測ると **Pico.css が当たっていない素の HTML** を測ることになり、数字が意味を失う。
npm から同じ版（`@picocss/pico@1.5.10` / `qrious@4.0.2`）の実バイトを取り、
同じパスで返す控えを用意した。

**Google Fonts はわざと塞いだまま**にしてある。フィルタリングされた学校とまったく同じ状態になる。

### 計測器そのものを、わざと壊して確かめた

「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない（v5 §P4）。
既知の不良を並べたページを作り、**12項目すべてで期待どおりに検出・非検出**になることを
確認してから測った。

| 仕込んだもの | 期待 | 結果 |
|---|---|---|
| `#bbb` の文字（比 1.92） | 検出する | ✅ |
| グラデーション面の上の白文字（比 2.10） | 検出する | ✅ |
| `oklch()` の薄い文字（比 2.21） | 検出する | ✅ |
| 黒文字（比 21） | 検出しない | ✅ |
| 入力欄に打った薄い文字（比 1.92） | 検出する | ✅ |
| 薄いプレースホルダ（比 1.36） | 検出する | ✅ |
| 薄い選択肢（比 1.61） | 検出する | ✅ |
| 濃い入力欄 | 検出しない | ✅ |
| 30×20 のボタン | 検出する | ✅ |
| 60×60 のボタン | 検出しない | ✅ |
| チェックボックスを囲む 20px のラベル | 検出する | ✅ |
| テキスト入力の見出しラベル | 検出しない | ✅ |

**このうち後半4項目は、最初の計測では見ていなかった。**
入力欄に打った文字・プレースホルダ・選択肢は、要素の子ノードにならないため、
文字を数えるだけの計測では丸ごと落ちる。塞いだところ、改修前の件数が
25件から31件に増えた。**穴の分だけ「0件」が嘘になっていた。**

---
## 1. まとめ

| 区分 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満（要素） | **31件** | **0件** |
| タップ44px未満 | **73件** | **0件** |
| CDN から取る実行コード | 17.6KB | **0バイト** |
| CDN が塞がれたときの QR（塗られた画素） | **0** | **10000** |
| JS エラー | 0件 | 0件 |
| 内訳（コントラスト） | 記者 13 / 編集室 18 | 0 / 0 |
| 内訳（タップ） | 記者 0 / 編集室 73 | 0 / 0 |

設定の有無：

| 項目 | 記者ページ `index.html` | 新聞編集室 `admin.html` |
|---|---|---|
| `viewport-fit=cover` | 有り | 無し → **有り** |
| `Code.gs` の `addMetaTag` | **無し** → **有り**（両画面に共通） | 同左 |
| `safe-area-inset-*` | 有り | 無し → **有り** |
| `prefers-reduced-motion` | 有り | 無し → **有り** |
| `forced-colors` | 有り | 無し → **有り** |
| `:focus-visible` | 有り | 無し → **有り** |
| `touch-action` | 有り | 無し → **有り** |
| 320px 幅の横スクロール | 出ない | 操作パネルは**収まる**（紙面 794px は A4 の幅そのもの） |
| `role="dialog"` / Esc で閉じる | 該当なし | **無し**（未対応） |
| `aria-live` / `role="alert"` | 無し | **無し**（未対応） |
| `<img>` の `alt` | 該当なし | **無し**（未対応） |
| ルビの `<rp>` | **無し**（未対応） | 該当なし |

**前回（v4）の PR #1 は `index.html` だけを直し、`admin.html` は「未計測」のまま残っていた。**
同じ書き方が使われている可能性が高い、という前回の見立ては当たっていた。

**`index.html` にも、前回測れていなかった画面に基準未満が残っていた。**
送信後のオーバーレイ、タグを選んだあとの状態、そしてプレースホルダである。

なお、31件のうち6件は**計測器の穴を塞いで初めて見えた**ものである。
入力欄に打った文字とプレースホルダは要素の子ノードにならないため、
文字を数えるだけの計測では丸ごと落ちる。塞いだうえで**改修前の版を取り出して
測り直した**（25件 → 31件）。測っていない状態について「壊れていた」とは書かない（v5 §7-6）。

---

## 2. 監査表 A〜G

判定は ✅ 満たす / ❌ 満たさない / ➖ 該当なし / **未計測**。
**「未計測」は ✅ ではない。**
矢印は今回の改修による変化を示す。

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT、リポジトリ直下 |
| A2 | `.gitignore` | ❌ → ✅ | 無かった。`.clasp.json` / `.env` / `node_modules` を対象に追加 |
| A3 | `dependabot.yml` | ⚠️ → ✅ | 有ったが `github-actions` 対象でワークフローが1本も無く、何も更新していなかった。CI を置いて機能するようになった |
| A4 | README / MANUAL / AUDIT | ✅ | 3点とも有り |
| A5 | CI ワークフロー | ❌ → ✅ | `pull_request` と `push` の両方で動く。品質ゲートとその自己検証を回す |

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ❌ | 未導入。**GAS はコードを `.html` にインラインで持つ構造のため `script-src 'self'` を入れると全部止まる。本番でしか確かめられない**（§7） |
| B2 | 秘密情報・IDの直書きなし | ✅ | `.clasp.json` / `.env` なし。シートIDもフォルダIDも `PropertiesService` 経由。品質ゲートで継続的に見る |
| B3 | OAuth スコープ最小 | 未計測 | `appsscript.json` がリポジトリに無く、実際の要求スコープを確認できない |
| B4 | `postMessage` の宛先が `*` でない | ➖ | `postMessage` を使っていない |
| B5 | サーバー側5段ガード | ❌ | 認証が無い。`?p=admin` を開けば誰でも編集室に入れる。**児童の氏名・写真を扱うため要判断**（§7） |
| B6 | CDN から取る実行コードが 0 | ❌ → ✅ | QRious 17.6KB を cdnjs から読んでいた。MIT の qr-creator 12.0KB を同梱し **0バイト**にした（§5） |
| B7 | 残る外部資産に SRI と版の固定 | ⚠️ | 残る外部参照は Pico.css（版は固定、`integrity` 無し）と Google Fonts のみ。どちらも塞がれても動作に影響しない見た目だけのもの |

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | ✅ | `saveArticle` 10秒待ち・`updateArticleTag` 5秒待ち、いずれも `finally` で解放 |
| C2 | 自動復旧（シート再生成） | ✅ | `Articles` / `SystemData` とも無ければ作る。列はヘッダー名で探し、無ければ既定位置にフォールバック |
| C3 | `pagehide` で記録確定 | ❌ | 無し。**編集室の作業内容はブラウザ内にしか無い**ので、Chromebook のタブ破棄で消える |
| C4 | 通信失敗時のリトライと明示 | ✅ | 記者ページは最大3回再送し、待機中を画面に出す。編集室は失敗をトーストで出す |
| C5 | `localStorage.clear()` を使っていない | ✅ | `localStorage` 自体を使っていない |

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に `viewport-fit=cover` | ❌ → ✅ | 3箇所中1箇所だけだった。`admin.html` と `Code.gs` の `addMetaTag` にも入れた（§6） |
| D2 | `100dvh` を使用 | ⚠️ | `index.html` に `.full-viewport` の定義はあるが、**どの要素にも付いていない**。両画面とも画面いっぱいに広げる要素が無いため、当てる先が無い |
| D3 | `safe-area-inset` を適用 | ⚠️ → ✅ | `index.html` のみ4箇所だった。`admin.html` にも入れた |
| D4 | `clamp()` による fluid type | ❌ | 両画面とも固定 px / rem のみ。**未対応** |
| D5 | Canvas に DPR 補正 | ❌ → ✅ | QR が 50×50 の実画素のままだった。実画素を 2 倍に持つようにした（§5） |
| D6 | 320px 幅で横スクロールが出ない | ⚠️ | 記者ページ ✅。編集室は操作パネルが 814px → **320px に収まる**ようになった。紙面は A4（794px）そのものなので残る |
| D7 | 画像に width/height、150KB以下 | ⚠️ | リポジトリ内に画像ファイルは無い。紙面の `<img>` は CSS で寸法を与えており、`width`/`height` 属性・`loading` は無い。**未対応** |
| D8 | コントラスト 4.5:1 以上 | ❌ → ✅ | **31要素 → 0要素**（§3） |
| D9 | タップ領域 44px 以上 | ❌ → ✅ | **73箇所 → 0箇所**（§4） |
| D10 | `prefers-reduced-motion`（`.01ms`） | ⚠️ → ✅ | `admin.html` に無かった。`.01ms` で入れた（`0` にすると `fill-mode: forwards` が壊れる） |
| D11 | `forced-colors` 対応 | ⚠️ → ✅ | `admin.html` に無かった |
| D12 | 提示モード | ➖ | 一斉授業で提示する画面ではない（先生が編集し、印刷して配る） |
| D13 | 印刷CSS | ✅ | 改修前後で紙面は 794×1123・記事3件と同一（実測） |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` とも無し |

### E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1〜E13 | PWA 一式 | ❌ | **未着手**。manifest / sw.js / offline.html / install-hook.js / icons のいずれも無い |

C型（GAS ウェブアプリ）は `script.google.com` から iframe で配信されるため、
**このリポジトリ単体では PWA にできない**（§3-5 の表）。
GitHub Pages 側にシェルを置く C+型への変更が要る。**構成の変更なので別途判断が要る。**

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ⚠️ | 絵文字だけの削除ボタン2つに `aria-label` を付けた。**紙面の `<img>` の `alt` と、トーストの `aria-live` は未対応** |
| F2 | モーダルに `role="dialog"`・Esc で閉じる | ❌ | モーダル6つとも `role` 無し・Esc 無し・フォーカスの閉じ込め無し。**未対応** |
| F3 | キーボードのみで全機能に到達 | ⚠️ | `:focus-visible` を入れて、いまどこにいるかは見えるようになった。**並び替えはドラッグのみで、キーボードの代替手段が無い（未対応）** |
| F4 | `rt` の色を決め打ちしていない | ⚠️ → ✅ | タグのラベルに残っていた決め打ちを外し、継がせる指定に任せた。品質ゲートで継続的に見る |
| F5 | 初回JS 300KB以下 | ✅ | 外部から取る実行コードは 0。同梱の qr-creator 12.0KB を含めて 101KB |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大 `admin.html` 1,077行 / 74.6KB |

### G. 学習ログ

| # | 項目 | 判定 |
|---|---|:--:|
| G1・G2 | `study.v1` | ➖ 学習系ではない（記事の投稿と組版） |

---

## 3. コントラスト（改修前 31要素 / 原因 20箇所 → 0）

比は実フォントサイズでの実測値。要求値は 4.5（大きな文字＝24px以上、
または 18.66px以上かつ太字は 3.0）。

### 記者ページ `index.html` — 13要素

| 対象 | 前 | 後 | 比 | どういう文か |
|---|---|---|---:|---|
| 入力欄のプレースホルダ3つ | `#888` | `#5f6368` | **3.54 → 6.05** | 「例：運動会で練習の成果を出せたよ」「いつ、どこで、だれが、なにをしたかな？」「自分の名前を入力」 |
| 送信完了の説明文 `.success-sub`（＋その中のルビ5つ） | `#7f8c8d` | `#5a6673` | **3.21 → 5.41** | 「先生のパソコンに届きました。3秒後に戻ります。」 |
| 選択中タグのふりがな `rt` | `rgba(255,255,255,.9)` の決め打ち | 決め打ちを外し面を濃く | **3.52 → 5.30** | 選んだタグの上のふりがな |

**通常の入力画面（1366px / 320px とも）は改修前から 0件。** 前回の PR #1 で直した範囲は保たれていた。
今回出たのは、前回「測れていないもの」に挙げていた**送信後の画面**、
**タグを選んだあとの状態**、そして**計測器の穴で見えていなかったプレースホルダ**である。

プレースホルダは「ここに何を書けばよいか」の合図で、**いちばん助けが要る子に向けた文**である。
装飾ではなく本文として測った（v5 §2-8）。

ふりがなの件は §4 と同じ形が1箇所だけ残ったものである。
**PR #1 は「色のついた面では色を継がせる」形に直したが、タグのラベルには
`rgba(255,255,255,0.9)` の決め打ちが先に書かれており、そちらが残っていた。**
この面（`#3498db`）は白を継がせても 11.4px のふりがなでは 4.5 に届かないため、
**ふりがなの色ではなく、面の色を 1段濃く**した（`#2471a3`）。

### 新聞編集室 `admin.html` — 18要素

| 対象 | 前 | 後 | 比 |
|---|---|---|---:|
| 🖨️ 印刷 / PDF保存 | `#2ecc71`→`#27ae60` | `#1e8449`→`#186a3b` | **2.10 → 4.72** |
| 日付範囲の「～」 | `#aaa` | `#5f6368` | **2.32 → 6.05** |
| テンプレート「適用」 | `#e67e22` | `#af601a` | **2.85 → 4.66** |
| テンプレート「保存」／トースト（成功） | `#27ae60` | `#1e8449` | **2.87 → 4.72** |
| 文字サイズの現在値「12px」 | `#3498db` | `#2471a3` | **2.99 → 5.02** |
| 🔄 更新／モーダルの確定4つ／トースト（情報）／QR の URL 欄 | `#3498db` | `#2471a3` | **3.15 → 5.30** |
| タグ行の削除「×」／トースト（エラー） | `#e74c3c` | `#c0392b` | **3.82 → 5.44** |
| 記事一覧の記者名 | `#777` | `#6b6b6b` | **4.39 → 5.23** |

**いちばん重かったのは「🖨️ 印刷 / PDF保存」の 2.10。** この画面でいちばん大きく、
いちばん最後に押すボタンである。緑のグラデーションに白文字を載せており、
**明るいほうの端で 2.10**。グラデーションは停止色をすべて取り、
いちばん比が小さくなる点で判定している。

**次に重かったのはトーストの3種すべて。** 「保存しました」「読み込みエラー」は、
**結果を伝えるためだけに出る文**で、3秒で消える。読めないと何が起きたか分からない。

色は**色相を変えず、面か文字を1〜2段濃くした**。面用と文字用を分けて変数に置いてある。

---

## 4. タップ領域（改修前 73箇所 → 0）

`index.html` は改修前から **0件**（前回 PR #1 で「編集ページへ」を 44px にした状態が保たれていた）。
以下はすべて `admin.html`。

| 前の大きさ | 対象 | 件数 | 直し方 |
|---|---|---:|---|
| 24×23 | タグ設定の削除「×」 | 1 | 大きさそのものを 44px に |
| 40×27 | タグ設定の絵文字入力 | 5 | 幅・高さとも 44px に |
| 65×25 | 🔄 更新 | 1 | 疑似要素 |
| 60×28 / 73×28 | 記事ごとの画像セレクト | 6 | 高さを確保 |
| 51×34 / 51×36 | テンプレートの保存・適用 | 2 | 疑似要素 |
| 55×42 | 日付フィルタのクリア | 1 | 疑似要素 |
| 116×22 / 142×19 | チェックボックスの囲みラベル3種 | 6 | 囲みの `<label>` で高さを確保 |
| 37〜39px 高 | 上部バー5つ、モーダルの確定・取消 | 13 | 疑似要素 |
| 27〜36px 高 | 各種入力欄・セレクト・スライダー | 38 | 高さを確保 |

**いちばん小さかったのは 24×23 の「×」（タグを消すボタン）。**
押し間違えるとタグが消え、**保存すると児童の画面からタグが消える。**

**37〜39px のボタンは「あと 5〜7px」だった。**
`min-height: 44px` を直接当てると上部バーが折り返して別の破綻を生むので、
§2-9 のとおり**疑似要素で当たり判定だけを広げた**（見た目は変わらない）。

**チェックボックスとラジオ、入力欄、選択肢、スライダーには疑似要素が使えない**
（置換要素は疑似要素を持てない）。これらは高さそのものを確保している。
縦に積んである列なので、広げても折り返しは起きない。

---

## 5. 依存 — 学校で塞がれたときに何が起きるか

`@babel/standalone` と `cdn.tailwindcss.com` は **使っていない**（v5 の最重要項目は該当なし）。
外部参照は3つあり、**塞いだ状態と通した状態を実際に測った。**

| 参照先 | 種類 | 塞がれたときの実測 |
|---|---|---|
| `unpkg.com` Pico.css 1.5.10 | 見た目 | 記者ページは**動く**。タグ5個が出て、入力・送信ができる。字と余白が変わるだけ |
| `fonts.googleapis.com` | 見た目 | 端末側フォントに落ちる。動作に影響なし |
| ~~`cdnjs.cloudflare.com` QRious 4.0.2~~ | ~~実行コード~~ | **同梱に切り替えた（下記）** |

### QRious が塞がれたときの実測（改修前）

| | 通ったとき | 塞がれたとき |
|---|---:|---:|
| 記事一覧 | 4件 | 4件 |
| 紙面の記事 | 3件 | 3件 |
| QR の `<canvas>` | 1個 | 1個 |
| **canvas に塗られた画素** | **2500** | **0** |
| コンソール | なし | `QRious is not defined` |

**アプリ全体は動くが、QR だけが空の四角として残る。**
`<canvas>` と「LINK」の文字は先に置かれるため、**先生の画面には枠が出ており、
そのまま印刷すると「読み取れない白い四角」が紙面に刷られる。**

白い画面になるほうがまだ気づける。**これは気づけない形で壊れる。**

### 直し方と、ライセンスの問題

QRious を同梱すれば解決する、と考えて調べたところ **QRious は GPL-3.0** だった。
このリポジトリは MIT なので、**同梱して配布するとライセンスが衝突する。**

MIT の `qr-creator` 1.0.0 に差し替えた。**17.6KB → 12.0KB** と小さくもなる。

差し替えの正しさは、**描いた QR を実際に復号して**確かめた（jsQR）。
画素の一致で比べると版とマスクの違いで差が出るため、比較にならない。

| URL | 改修前 QRious 50px | qr-creator 50px | qr-creator 100px（採用） |
|---|:--:|:--:|:--:|
| `https://example.com/movie` | ✅ | ✅ | ✅ |
| ドライブの共有 URL（63文字） | ✅ | ✅ | ✅ |
| YouTube の URL | ✅ | ✅ | ✅ |
| GAS のデプロイ URL（73文字） | ✅ | **❌ 読めない** | ✅ |

**そのまま差し替えると、長い URL で退行する**ことが分かったので、
実画素を 2 倍に持つようにした（v5 §2-5 の DPR 補正。D5 として挙げていた欠落でもある）。

倍率を端末の `devicePixelRatio` で決めていない。**この canvas は印刷して配るもの**で、
先生が DPR 1 の PC で作って印刷すると 50×50 しか無い QR が刷られてしまうためである
（v5 §2-5 の「配る画像は固定値で決め打つ」）。

### 改修後の実測（外部を一切通さない状態）

| 項目 | 前 | 後 |
|---|---:|---:|
| canvas に塗られた画素 | **0** | **10000** |
| 実画素 | 50×50 | **100×100** |
| 見た目の大きさ | 50×50 | 50×50（紙面の割付は変わらない） |
| 復号できた URL | — | 4つとも ✅ |
| JS エラー | `QRious is not defined` | **0件** |

---

## 6. GAS 固有（§5）

### viewport が3箇所にあり、1箇所だけ直っていた

改修前：

```javascript
// Code.gs:119
.addMetaTag('viewport', 'width=device-width, initial-scale=1')          // ← 漏れていた
```

```html
<!-- index.html:8 -->
<meta name="viewport" content="... viewport-fit=cover">                  <!-- 有り -->
<!-- admin.html:6 -->
<meta name="viewport" content="width=device-width, initial-scale=1.0">   <!-- 漏れていた -->
```

v5 §5 は「GAS は画面を iframe で包むため、`index.html` と `code.gs` の**両方**に要る」と
書いている。前回の PR #1 は `index.html` だけを直しており、**残り2箇所が漏れていた。**

`addMetaTag` はサーバー側の処理なので、`index.html` を手元で組み立てても再現されない。
**どちらが勝つかは本番でしか確かめられない（未計測）。**
そのため「効いていない」とは書かない。**3箇所すべてに書けば、どれが勝っても正しくなる。**

品質ゲートは `<meta>` と `addMetaTag` の両方を見るので、片方だけ直すと落ちる。

### `include()` を使っていない

CSS も JavaScript も `index.html` / `admin.html` に直接書かれている。
GAS としては動くが、**CSP を入れる段（§2-13）で必ず書き直しになる。**

---

## 7. 未計測・判断が要る点

### 測っていないもの

- **本番の URL・デプロイ状態**（`script.google.com` へ到達できない）
- **サーバー側の戻り値に依存する挙動**（実際の保存・読み出し・排他ロックの競合）
- **`appsscript.json` の OAuth スコープ**（リポジトリに無い）
- **印刷した紙で QR を実際に読ませた結果**（画面上での復号は確認済み）
- **`Code.gs` の `addMetaTag` と HTML の `<meta>` のどちらが勝つか**
- **iPad 実機・Chromebook 実機での表示**（Chromium で寸法を合わせた計測のみ）

### 直していないもの（今回の範囲外）

- モーダル6つの `role="dialog"` / Esc で閉じる / フォーカスの閉じ込め
- 紙面の `<img>` の `alt`、トーストの `aria-live`
- ルビの `<rp>`（読み上げ時に二重に読まれる）
- 並び替えのキーボード代替手段（いまはドラッグのみ）
- `clamp()` による fluid type
- `pagehide` での確定保存（Chromebook のタブ破棄で編集内容が消える）
- 紙面が 320px 画面で横スクロールする点（A4 の幅そのものなので、
  直すと WYSIWYG でなくなる）

### 人間が決めること

1. ~~**編集室に認証が無い。**~~ → **2026-08-08 の PR #3 で塞いだ。**
   `TEACHER_EMAILS` との照合を、画面の入口と管理系サーバー関数の両方で通している。
   記者ページの「編集ページへ」も、先生のアカウントで開いたときだけ出る。
   **この項目は 2026-08-23 まで「残っている」と書かれたままだった。**
   直したときに AUDIT を更新していなかったため、公開している紹介記事にも
   「編集室に合言葉がない」と書かれ続けていた（同日に直した）。
2. ~~**投稿画像が「リンクを知っている全員が閲覧可」で保存される。**~~
   → **2026-08-08 の PR #3 で `DOMAIN_WITH_LINK`（＋不可なら `PRIVATE`）に変えた。**
   これも 1 と同じく、記録の更新が漏れていた。
3. **CSP を入れるか。** GAS はコードを `.html` にインラインで持つ構造なので、
   `script-src 'self'` を入れると**全部止まる**。`include()` への分割が先に要り、
   さらに `HtmlService` 独自のサンドボックスの中でどう働くかは**本番でしか確かめられない**。
4. **PWA 化するか。** C型のままでは不可能で、GitHub Pages 側にシェルを置く C+型への
   構成変更が要る（v5 §3-5）。
5. **`Status` 列を使うか。** `Pending` を書き込むだけで、読む処理も更新する処理も無い。
   承認フローを作るのか、列ごと畳むのかは仕様の判断。

### この点検で分かった、他リポジトリにも効くこと

- **「1箇所ずつ潰した」あとの取りこぼしは、状態を変えないと見えない。**
  ふりがなの件は通常表示では 0件で、**タグを選ぶまで出てこなかった**。
  §7-1 の「操作して測る」を飛ばすと、直したつもりのものが残る。
- **計測器そのものに穴があると、その穴の分だけ「0件」が嘘になる。**
  入力欄に打った文字とプレースホルダは要素の子ノードにならないため、
  文字を数えるだけの計測では丸ごと落ちる。**実際に6件が見えていなかった。**
  塞ぐ前と後で、同じ改修前の版が 25件 → 31件になった。
- **CDN の実行コードは、白い画面にならない壊れ方をすることがある。**
  QRious は「空の canvas」を残すので、**先生の画面上は正常に見えて、紙だけが壊れる。**
- **自己ホスト化はライセンスを先に見る。** QRious は GPL-3.0 で、
  MIT のリポジトリには同梱できなかった。CDN から読むだけなら問題にならないが、
  **同梱に切り替えた瞬間に配布物になる。**
- **ライブラリを差し替えるときは、出力そのものを検証する。**
  画素の一致で比べると版やマスクの違いで差が出て比較にならない。
  QR は**復号して元の文字列に戻るか**で見た。そうしたら、素朴な差し替えでは
  長い URL が読めなくなることが分かった。
- **前回の PR で「未計測」と書いた画面は、次に必ず測る。**
  `admin.html` は前回そう書いてあり、実際にコントラスト18件・タップ73件が出た。

---

## 付録. 前回（v4, 2026-08-03）の記録

PR #1 でマージ済み。`index.html` のみが対象で、`admin.html` は未計測だった。

| 区分 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満 | 5件 | 0件 |
| タップ44px未満 | 1件 | 0件 |
| `viewport-fit=cover` | 無し | 有り（`index.html` のみ） |
| `env(safe-area-inset-*)` | 無し | 有り |
| `100dvh` | 無し | 定義のみ（未適用） |
| `prefers-reduced-motion` | 無し | 有り |
| `forced-colors` | 無し | 有り |
| LICENSE / dependabot | 無し | 追加 |

### いちばん重かった件：ふりがなが送信ボタンの上で読めなかった

`rt { color: #555 }` と決め打ちしていたため、**色のついた送信ボタンの上で比 1.47**。
ふりがなが必要なのは低学年の児童で、**いちばん読めなくて困る人が、いちばん読めない**
形になっていた。色のついた面ではまとめて色を継がせるように直した。

```css
rt { color: #5f6368; }
button rt, .tag-label rt, .success-title rt, [class*="btn"] rt { color: inherit; }
```

**このとき、タグのラベルに先に書かれていた `rgba(255,255,255,0.9)` の決め打ちが残った。**
今回の §3 で比 3.52 として検出されている。

---

## 第3回（2026-08-23）— コンテナバインド配布と、シートの自動点検

計測はしていない。**ブラウザ実測の手段がこの環境に無いため、見た目は一切触っていない。**
触ったのは配り方（`appsscript.json` の `webapp`）、`Code.gs` の書き込み経路、
シートの点検・修整、そして GitHub Pages のトップである。

### 直したもの

| 何を | 前 | 後 |
|---|---|---|
| ウェブアプリの実行者 | `USER_ACCESSING` | `USER_DEPLOYING`（児童はシート／ドライブへのアクセス権が不要に） |
| 記事の書き込み | 位置決め打ちの 8 要素配列 | **見出し名で組む**（列を並べ替えても正しい列に入る） |
| タグの書き換え | ID=0 列目 / Tag=7 列目へフォールバック | 見出し名。無ければ足す。見つからなければ例外で止める |
| 記事一覧の読み取り | 見出しが無ければ既定の位置に落とす | **落とさない**（無い列は空。先生のメモ欄をタグとして読まない） |
| 保存時のロック | 画像の保存ごと囲んでいた（1 件数秒） | `appendRow` の 1 回だけ（数十 ms） |
| `getSystemSheet` | 公開関数（`google.script.run` から呼べた） | `getSystemSheet_` |
| シートの作り | 記事シートだけ、初回投稿時に作る | 2 枚とも自動でそろえる＋点検／修整メニュー |
| GAS の児童ページ | `index.html` | `post.html`（`index.html` は導入案内ページへ） |
| ゲート（`deploy.yml` 用） | `package.json` に `quality`/`ci`/`check` が無く**飛ばされていた** | `check` / `test` / `ci` を用意 |

### 測れたもの・測れなかったもの

| 項目 | 結果 |
|---|---|
| 品質ゲート `npm run check` | ✅ 11 本・指摘 0 件（作業前は 10 本・0 件。増えたのは新しい `index.html`） |
| テスト `npm test` | ✅ 32/32（作業前 11/11。足したのは `tests/schema.test.mjs` の 21 件） |
| 正本とのずれ `check-drift.mjs` | ✅ 正本と一致しています（**2 ファイル**）— 作業前と同じ |
| `Code.gs` の構文 | ✅ `node:vm` で解析可 |
| **GAS 上での実挙動** | ❌ **未確認。** この環境に GAS ランタイムは無く、外部通信も塞がれている |
| **`USER_DEPLOYING` での `Session.getActiveUser()`** | ❌ **未確認。** 同一 Workspace ドメインならメールが取れる、という Google の記述に依拠している |
| ブラウザ実測（コントラスト・タップ・ms） | ❌ **未計測。** 手段が無いので、見た目は 1 か所も変えていない |

### テストが本当に見張っているかを、わざと壊して確かめた

「0 件でした」だけでは、検査が動いているのか何も見ていないのか区別できない（v5 §P4）。
`Code.gs` を 2 通りに壊して、落ちることを確かめた。

| 壊した場所 | 結果 |
|---|---|
| 記事の書き込みを位置決め打ちに戻す | ❌ 2 件失敗（「列を並べ替えたシートでも、記事は正しい列に入る」ほか） |
| 「1 行目が見出しに見えない」判定を無効化 | ❌ 1 件失敗（「1 行目がデータになっていたら、何も書き換えずに人へ回す」） |

いずれも戻したあとは 32/32。

### 分かったこと

**AUDIT の「人間が決めること」は、直したときに消さないと嘘になる。**
第 7 節の 1 と 2（編集室に認証が無い／画像がリンクを知っている全員に公開）は
2026-08-08 の PR #3 で塞がれていたのに、記録は「残っている」のままだった。
その記述が公開中の紹介記事にもそのまま載り、**実際より危険なアプリとして 2 週間説明されていた。**
残っているものを書くのと同じだけ、消えたものを消す手間が要る。

**「見出し名で読む」と書いてあっても、書き込みが位置決め打ちなら意味がない。**
README には以前から「列はヘッダー名で探します。列を入れ替えても動きます」と書いてあった。
読み取りはそのとおりだったが、**`saveArticle` は 8 要素の配列をそのまま `appendRow` していた。**
先生が列を 1 本入れ替えると、次に届いた記事から本文が記者名の列に入る。
画面には何も出ず、印刷するまで誰も気づかない。**読みと書きは別に確かめること。**

**「見つからなければ既定の位置にフォールバック」は、親切ではなく事故である。**
`Tag` の見出しが無いシートで 7 列目（0 起点）を読むと、先生が右に足したメモ欄が
そのままタグとして記事一覧に並ぶ。無いものは無いと言ったほうが、点検で気づける。
