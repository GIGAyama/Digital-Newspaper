# デジタル新聞 点検記録（GIGA Standard v5）

2026-08-08 時点。数字はすべて**実測**。測っていないものは「未計測」と書く。

- [測り方](#測り方)
- [1. まとめ](#1-まとめ)
- [2. 監査表 A〜G](#2-監査表-ag)
- [3. コントラスト（実測 25要素 / 原因 14箇所）](#3-コントラスト実測-25要素--原因-14箇所)
- [4. タップ領域（実測 73箇所）](#4-タップ領域実測-73箇所)
- [5. 依存 — 学校で塞がれたときに何が起きるか](#5-依存--学校で塞がれたときに何が起きるか)
- [6. GAS 固有（§5）](#6-gas-固有5)
- [7. 未計測・判断が要る点](#7-未計測判断が要る点)
- [付録. 前回（v4, 2026-08-03）の記録](#付録-前回v4-2026-08-03の記録)

---

## 測り方

本番（`script.google.com`）へは作業環境から到達できないため、
**GAS が返す画面と同じものを手元で組み立てて**測った（v5 §7-3）。

- スクリプトレット（`<?= ScriptApp.getService().getUrl() ?>`）は畳む
- `google.script.run` は、戻り値の見本を返すダミーに差し替える
  （記事4件・タグ5件・保存データ2件。うち2件は画像あり）
- 色は「1px 実際に塗って `getImageData` で読む」方式（v5 §7-2）。
  `oklch()` や `color-mix()` でも壊れない
- グラデーションは停止色をすべて取り、**いちばん比が小さくなる点**で判定する
- 絵文字のみの要素、`disabled` / `cursor: not-allowed` は対象外（WCAG 対象外）
- 見出しラベル（`<label for>` がテキスト入力を指すだけのもの）はタップ対象から除く。
  押す本体は入力欄のほうであるため。**チェックボックス／ラジオを囲むラベルは残す**（§2-9）

計測環境：Chromium 141（Playwright）、1366×768（Chromebook GIGA標準機）と
320×568（設計の下限）、DPR 2。

### CDN の控えを用意してから測った（§7-4）

作業環境から `unpkg.com` / `cdnjs.cloudflare.com` へは出られない。
そのまま測ると **Pico.css が当たっていない素の HTML** を測ることになり、数字が意味を失う。
npm から同じ版（`@picocss/pico@1.5.10` / `qrious@4.0.2`）の実バイトを取り、
同じパスで返す控えを用意した。

**Google Fonts はわざと塞いだまま**にしてある。フィルタリングされた学校とまったく同じ状態になる。

### 計測器そのものを、わざと壊して確かめた

「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない（v5 §P4）。
既知の不良を並べたページを作り、**8項目すべてで期待どおりに検出・非検出**になることを確認してから測った。

| 仕込んだもの | 期待 | 結果 |
|---|---|---|
| `#bbb` の文字（比 1.92） | 検出する | ✅ |
| グラデーション面の上の白文字（比 2.10） | 検出する | ✅ |
| `oklch()` の薄い文字（比 2.21） | 検出する | ✅ |
| 黒文字（比 21） | 検出しない | ✅ |
| 30×20 のボタン | 検出する | ✅ |
| 60×60 のボタン | 検出しない | ✅ |
| チェックボックスを囲む 20px のラベル | 検出する | ✅ |
| テキスト入力の見出しラベル | 検出しない | ✅ |

---

## 1. まとめ

| 区分 | 記者ページ<br>`index.html` | 新聞編集室<br>`admin.html` |
|---|---:|---:|
| コントラスト基準未満（要素） | **7件** | **18件** |
| タップ44px未満 | **0件** | **73件** |
| 320px 幅で横スクロール | 出ない | **出る（814px）** |
| `viewport-fit=cover` | 有り | **無し** |
| `safe-area-inset-*` | 有り | **無し** |
| `prefers-reduced-motion` | 有り | **無し** |
| `forced-colors` | 有り | **無し** |
| `:focus-visible` | 有り | **無し** |
| `touch-action` | 有り | **無し** |
| `role="dialog"` / Esc で閉じる | 該当なし | **無し**（モーダル6つ） |
| `aria-live` / `role="alert"` | 無し | **無し**（トースト） |
| `<img>` の `alt` | 該当なし | **無し** |
| ルビの `<rp>` | **無し** | 該当なし |

**前回（v4）の PR #1 は `index.html` だけを直し、`admin.html` は「未計測」のまま残っていた。**
今回それを測った結果が上の右列である。同じ書き方が使われている可能性が高い、
という前回の見立ては当たっていた。

**`index.html` にも、前回測れていなかった画面（送信後・タグ選択後）に基準未満が残っていた。**

---

## 2. 監査表 A〜G

判定は ✅ 満たす / ❌ 満たさない / ➖ 該当なし / **未計測**。
**「未計測」は ✅ ではない。**

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT、リポジトリ直下 |
| A2 | `.gitignore` | ❌ | **ファイルが無い**（`.clasp.json` / `.env` の混入を止めるものが無い） |
| A3 | `dependabot.yml` | ⚠️ | 有り。ただし `github-actions` のみを対象にしており、**ワークフローが1本も無い**ので現状は何も更新しない |
| A4 | README / MANUAL / AUDIT | ✅ | 3点とも有り（README・MANUAL は PR #2 で作成） |
| A5 | CI ワークフロー | ❌ | `.github/workflows/` が無い。PR 時点の検査ができない |

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ❌ | 未導入。**GAS はコードを `.html` にインラインで持つ構造のため `script-src 'self'` を入れると全部止まる。本番でしか確かめられない**（§7 参照） |
| B2 | 秘密情報・IDの直書きなし | ✅ | `git ls-files` に `.clasp.json` / `.env` なし。シートIDもフォルダIDも `PropertiesService` 経由 |
| B3 | OAuth スコープ最小 | 未計測 | `appsscript.json` がリポジトリに無く、実際の要求スコープを確認できない |
| B4 | `postMessage` の宛先が `*` でない | ➖ | `postMessage` を使っていない |
| B5 | サーバー側5段ガード | ❌ | 認証が無い。`?p=admin` を開けば誰でも編集室に入れる。**児童の氏名・写真を扱うため要判断**（§7） |
| B6 | CDN から取る実行コードが 0 | ❌ | **QRious 17.6KB を cdnjs から**（§5） |
| B7 | 残る外部資産に SRI と版の固定 | ❌ | Pico.css・QRious とも版は固定されているが `integrity` が無い |

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | ✅ | `saveArticle` 10秒待ち・`updateArticleTag` 5秒待ち、いずれも `finally` で解放 |
| C2 | 自動復旧（シート再生成） | ✅ | `Articles` / `SystemData` とも無ければ作る。列はヘッダー名で探し、無ければ既定位置にフォールバック |
| C3 | `pagehide` で記録確定 | ❌ | 無し。**編集室の作業内容はブラウザ内にしか無い**ので、Chromebook のタブ破棄で消える |
| C4 | 通信失敗時のリトライと明示 | ✅ | 記者ページは最大3回再送し、待機中を画面に出す。編集室は失敗をトーストで出す |
| C5 | `localStorage.clear()` を使っていない | ✅ | `localStorage` 自体を使っていない |

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に `viewport-fit=cover` | ❌ | `index.html` のみ有り。**`admin.html` と `Code.gs` の `addMetaTag` に無い**（§6） |
| D2 | `100dvh` を使用 | ❌ | `index.html` に `.full-viewport` の定義はあるが、**どの要素にも付いていない**。`admin.html` は未使用 |
| D3 | `safe-area-inset` を適用 | ⚠️ | `index.html` のみ4箇所。`admin.html` は 0 |
| D4 | `clamp()` による fluid type | ❌ | 両画面とも固定 px / rem のみ |
| D5 | Canvas に DPR 補正（上限2） | ❌ | **QR の canvas が 50×50 の実画素のまま**（DPR 2 の端末で CSS 50px に等倍）。§5 に実測 |
| D6 | 320px 幅で横スクロールが出ない | ⚠️ | `index.html` ✅ / **`admin.html` は 814px まで広がる** |
| D7 | 画像に width/height、150KB以下 | ⚠️ | リポジトリ内に画像ファイルは無い。紙面の `<img>` は CSS で寸法を与えており、`width`/`height` 属性・`loading` は無い |
| D8 | コントラスト 4.5:1 以上 | ❌ | **25要素が基準未満**（原因 14箇所）。§3 |
| D9 | タップ領域 44px 以上 | ❌ | **73箇所が 44px 未満**。§4 |
| D10 | `prefers-reduced-motion`（`.01ms`） | ⚠️ | `index.html` ✅（`0.01ms` で正しい）。**`admin.html` は無し**（モーダル 0.3s・トースト 0.3s・カード hover が動く） |
| D11 | `forced-colors` 対応 | ⚠️ | `index.html` のみ。`admin.html` は無し |
| D12 | 提示モード | ➖ | 一斉授業で提示する画面ではない（先生が編集し、印刷して配る） |
| D13 | 印刷CSS | ✅ | `@media print` で操作パネル・モーダル・トーストを隠し、`@page` で A4 縦横を切り替える。`print-color-adjust: exact` も有り |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` とも無し |

### E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1〜E13 | PWA 一式 | ❌ | **未着手**。manifest / sw.js / offline.html / install-hook.js / icons のいずれも無い |

C型（GAS ウェブアプリ）は `script.google.com` から iframe で配信されるため、
**このリポジトリ単体では PWA にできない**（§3-5 の表）。
GitHub Pages 側にシェルを置く C+型への変更が要る。**構成の変更なので別途判断が要る。**

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ❌ | すべて 0件。紙面の `<img>` に `alt` 無し、絵文字だけのボタン（🗑️ ×）に `aria-label` 無し、トーストに `aria-live` 無し |
| F2 | モーダルに `role="dialog"`・Esc で閉じる | ❌ | モーダル6つとも `role` 無し・Esc 無し・フォーカスの閉じ込め無し |
| F3 | キーボードのみで全機能に到達 | ⚠️ | 並び替えはドラッグのみで**キーボードの代替手段が無い**。加えて `admin.html` に `:focus-visible` が無く、いまどこにいるかが見えない |
| F4 | `rt` の色を決め打ちしていない | ⚠️ | 既定値と「色のついた面では継がせる」指定は入っている（PR #1）。ただし**タグのラベルだけ `rgba(255,255,255,0.9)` の決め打ちが残っており、比 3.52**（§3） |
| F5 | 初回JS 300KB以下 | ✅ | 外部 JS は QRious 17.6KB のみ。自前のコードは全部で 88.8KB |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大 `admin.html` 924行 / 60.5KB |

### G. 学習ログ

| # | 項目 | 判定 |
|---|---|:--:|
| G1・G2 | `study.v1` | ➖ 学習系ではない（記事の投稿と組版） |

---

## 3. コントラスト（実測 25要素 / 原因 14箇所）

比は白地・実フォントサイズでの実測値。要求値は 4.5（大きな文字＝24px以上、
または 18.66px以上かつ太字は 3.0）。

### 記者ページ `index.html` — 7要素

| 対象 | 色 | 面 | 比 | どういう文か |
|---|---|---|---:|---|
| 送信完了の説明文 `.success-sub` | `#7f8c8d` | `#f4f6f8` | **3.21** | 「先生のパソコンに届きました。3秒後に戻ります。」＋その中のルビ5つ |
| 選択中タグのふりがな `rt` | `rgba(255,255,255,.9)` | `#3498db` | **3.52** | 選んだタグの上のふりがな |

**通常の入力画面（1366px / 320px とも）は 0件。** 前回の PR #1 で直した範囲は保たれている。
今回あらためて測ったのは、前回「測れていないもの」に挙げていた**送信後の画面**と、
**タグを選んだあとの状態**である。

ふりがなの件は §4 と同じ形が1箇所だけ残ったもの。
**PR #1 は「色のついた面では色を継がせる」形に直したが、タグのラベルには
`rgba(255,255,255,0.9)` の決め打ちが先に書かれており、そちらが残っていた。**
なお、この面（`#3498db`）は白を継がせても 11.4px のふりがなでは 4.5 に届かないため、
**ふりがなの色ではなく、選択中タグの面の色を濃くする**のが筋になる。

### 新聞編集室 `admin.html` — 18要素

| 対象 | 色 | 面 | 比 |
|---|---|---|---:|
| 🖨️ 印刷 / PDF保存 | `#fff` | `#2ecc71`（グラデ上端） | **2.10** |
| 日付範囲の「～」 | `#aaa` | `#fff` | **2.32** |
| テンプレート「適用」 | `#fff` | `#e67e22` | **2.85** |
| テンプレート「保存」 | `#fff` | `#27ae60` | **2.87** |
| トースト（成功） | `#fff` | `#27ae60` | **2.87** |
| 文字サイズの現在値「12px」 | `#3498db` | `#f8f9fa` | **2.99** |
| 🔄 更新 | `#fff` | `#3498db` | **3.15** |
| モーダルの確定ボタン4つ（設定を保存／追加／保存する／復元する） | `#fff` | `#3498db` | **3.15** |
| トースト（情報） | `#fff` | `#3498db` | **3.15** |
| タグ行の削除「×」 | `#fff` | `#e74c3c` | **3.82** |
| トースト（エラー） | `#fff` | `#e74c3c` | **3.82** |
| 記事一覧の記者名 | `#777` | `#f6fffa` / `#fff` | **4.39 / 4.48** |

**いちばん重いのは「🖨️ 印刷 / PDF保存」の 2.10。** この画面でいちばん大きく、
いちばん最後に押すボタンである。緑のグラデーション（`#2ecc71`→`#27ae60`）に白文字を載せており、
**明るいほうの端で 2.10**。

**次に重いのはトーストの3種すべて。** 「保存しました」「読み込みエラー」は、
**結果を伝えるためだけに出る文**で、3秒で消える。読めないと何が起きたか分からない。

「12px」の 2.99 も同じ性質で、**いま何 px かを示す唯一の表示**である。

---

## 4. タップ領域（実測 73箇所）

`index.html` は **0件**（前回 PR #1 で「編集ページへ」を 44px にした状態が保たれている）。
以下はすべて `admin.html`。

| 大きさ | 対象 | 件数 |
|---|---|---:|
| 24×23 | タグ設定の削除「×」 | 1 |
| 40×27 | タグ設定の絵文字入力 | 5 |
| 65×25 | 🔄 更新 | 1 |
| 60×28 / 73×28 | 記事ごとの画像セレクト（大・形・配置） | 6 |
| 51×34 / 51×36 | テンプレートの保存・適用 | 2 |
| 55×42 | 日付フィルタのクリア | 1 |
| 116×22 / 142×19 | 「見出し隠す」「選択中のみ表示」「並び替えモード」のチェック囲み | 6 |
| 37〜39px 高 | 上部バーの4ボタン＋投稿画面へ、モーダルの確定・取消ボタン | 13 |
| 27〜36px 高 | 各種入力欄・セレクト（レイアウト設定8つ、タグ欄、QR欄、検索、日付、保存名ほか） | 38 |

**いちばん小さいのは 24×23 の「×」（タグを消すボタン）。**
押し間違えるとタグが消え、**保存すると児童の画面からタグが消える。**

**37〜39px のボタンは「あと 5〜7px」で、詰めて組んだツールバーを崩さずに直せる。**
§2-9 のとおり、疑似要素で当たり判定だけを広げるのが適している
（`min-height: 44px` を直接当てると上部バーが折り返す）。

**チェックボックス囲みの 19〜22px は、疑似要素が使えない**（`input` は疑似要素を持てない）。
囲みの `<label>` 側で高さを確保する必要がある。

---

## 5. 依存 — 学校で塞がれたときに何が起きるか

`@babel/standalone` と `cdn.tailwindcss.com` は **使っていない**（v5 の最重要項目は該当なし）。
残る外部参照は3つ。**塞いだ状態と通した状態を実際に測った。**

| 参照先 | 種類 | 塞がれたときの実測 |
|---|---|---|
| `unpkg.com` Pico.css 1.5.10 | 見た目 | 記者ページは**動く**。タグ5個が出て、入力・送信ができる。字と余白が変わるだけ |
| `fonts.googleapis.com` | 見た目 | 端末側フォントに落ちる。動作に影響なし |
| `cdnjs.cloudflare.com` QRious 4.0.2 | **実行コード** | **QR が黙って壊れる**（下記） |

### QRious が塞がれたときの実測

| | 通ったとき | 塞がれたとき |
|---|---:|---:|
| 記事一覧 | 4件 | 4件 |
| 紙面の記事 | 3件 | 3件 |
| QR の `<canvas>` | 1個 | 1個 |
| **canvas に塗られた画素** | **2500** | **0** |
| コンソール | なし | `QRious is not defined` |

**アプリ全体は動くが、QR だけが空の四角として残る。**
`<canvas>` と「LINK」の文字は先に置かれるため、**先生の画面には枠が出ており、
そのまま印刷すると「読み取れない白い四角」が紙面に刷られる。**

白い画面になるほうがまだ気づける。**これは気づけない形で壊れる。**

QRious は 17.6KB しかなく、`.html` に包んで自分側に持てば
**CDN から取る実行コードを 0 バイトにできる**（§8 の目標値）。

### QR の鮮明さ

| 項目 | 実測 |
|---|---|
| `canvas.width` × `canvas.height`（実画素） | 50 × 50 |
| CSS 上の大きさ | 50 × 50 px |
| `devicePixelRatio` | 2 |

**DPR 補正が無い**（§2-5）。画面上ですでに等倍で、印刷時は 96dpi 換算で約 13mm の QR に
50×50 の実画素しか無い。`level: 'L'` と合わせて、**読み取り成功率は未計測**だが余裕は無い。

---

## 6. GAS 固有（§5）

### viewport が2箇所にあり、片方だけ直っている

```javascript
// Code.gs:119
.addMetaTag('viewport', 'width=device-width, initial-scale=1')
```

```html
<!-- index.html:8 -->
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<!-- admin.html:6 -->
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

**`viewport-fit=cover` があるのは3箇所中1箇所だけ。**
v5 §5 は「GAS は画面を iframe で包むため、`index.html` と `code.gs` の**両方**に要る」と
書いている。前回の PR #1 は `index.html` だけを直しており、**`Code.gs` が漏れていた。**

`addMetaTag` はサーバー側の処理なので、`index.html` を手元で組み立てても再現されない。
**どちらが勝つかは本番でしか確かめられない（未計測）。**
そのため「効いていない」とは書かない。**両方に書けば、どちらが勝っても正しくなる。**

### `include()` を使っていない

CSS も JavaScript も `index.html` / `admin.html` に直接書かれている。
GAS としては動くが、**CSP を入れる段（§2-13）で必ず書き直しになる。**

---

## 7. 未計測・判断が要る点

### 測っていないもの

- **本番の URL・デプロイ状態**（`script.google.com` へ到達できない）
- **サーバー側の戻り値に依存する挙動**（実際の保存・読み出し・排他ロックの競合）
- **`appsscript.json` の OAuth スコープ**（リポジトリに無い）
- **QR の実際の読み取り成功率**（印刷して読ませていない）
- **`Code.gs` の `addMetaTag` と HTML の `<meta>` のどちらが勝つか**
- **iPad 実機・Chromebook 実機での表示**（Chromium で寸法を合わせた計測のみ）

### 人間が決めること

1. **編集室に認証が無い。** `?p=admin` を知っていれば誰でも入れ、
   記者ページの下部にはそのリンクが置かれている。児童の氏名と写真を扱うので、
   デプロイの「アクセスできるユーザー」だけで足りるかは**運用側の判断が要る**。
2. **投稿画像が「リンクを知っている全員が閲覧可」で保存される**（`Code.gs:175`）。
   紙面プレビューに出すためだが、URL が漏れれば校外からも見える。
3. **CSP を入れるか。** GAS はコードを `.html` にインラインで持つ構造なので、
   `script-src 'self'` を入れると**全部止まる**。`include()` への分割が先に要り、
   さらに `HtmlService` 独自のサンドボックスの中でどう働くかは**本番でしか確かめられない**。
4. **PWA 化するか。** C型のままでは不可能で、GitHub Pages 側にシェルを置く C+型への
   構成変更が要る（v5 §3-5）。
5. **`Status` 列を使うか。** `Pending` を書き込むだけで、読む処理も更新する処理も無い。
   承認フローを作るのか、列ごと畳むのかは仕様の判断。

### この点検で分かった、他リポジトリにも効くこと

- **「1箇所ずつ潰した」あとの取りこぼしは、状態を変えないと見えない。**
  ふりがなの件は通常表示では 0件で、**タグを選ぶまで出てこなかった**。
  §7-1 の「操作して測る」を飛ばすと、直したつもりのものが残る。
- **CDN の実行コードは、白い画面にならない壊れ方をすることがある。**
  QRious は「空の canvas」を残すので、**先生の画面上は正常に見えて、紙だけが壊れる。**
- **前回の PR で「未計測」と書いた画面は、次に必ず測る。**
  `admin.html` は前回そう書いてあり、実際にコントラスト18件・タップ73件が出た。

---

## 付録. 前回（v4, 2026-08-03）の記録

PR #1 でマージ済み。`index.html` のみが対象で、`admin.html` は未計測だった。

| 区分 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満 | 5件 | 0件 |
| タップ44px未満 | 1件 | 0件 |
| `viewport-fit=cover` | 無し | 有り（`index.html` のみ） |
| `env(safe-area-inset-*)` | 無し | 有り |
| `100dvh` | 無し | 定義のみ（未適用） |
| `prefers-reduced-motion` | 無し | 有り |
| `forced-colors` | 無し | 有り |
| LICENSE / dependabot | 無し | 追加 |

### いちばん重かった件：ふりがなが送信ボタンの上で読めなかった

`rt { color: #555 }` と決め打ちしていたため、**色のついた送信ボタンの上で比 1.47**。
ふりがなが必要なのは低学年の児童で、**いちばん読めなくて困る人が、いちばん読めない**
形になっていた。色のついた面ではまとめて色を継がせるように直した。

```css
rt { color: #5f6368; }
button rt, .tag-label rt, .success-title rt, [class*="btn"] rt { color: inherit; }
```

**このとき、タグのラベルに先に書かれていた `rgba(255,255,255,0.9)` の決め打ちが残った。**
今回の §3 で比 3.52 として検出されている。
