# ロールアウト記録 — Digital-Newspaper

GIGA Standard v5 に沿った改修の記録。
**他リポジトリにも効く知見**を残すための場所であって、進捗の一覧ではない。

| 回 | 日付 | 版 | 範囲 | PR |
|---|---|---|---|---|
| 1 | 2026-08-03 | v4 | `index.html` のみ。ふりがな 1.47、コントラスト 5→0件、タップ 1→0件 | #1（マージ済み） |
| 2 | 2026-08-08 | v5 | `admin.html` を初めて実測。P0 / P0.5 / P1 | #3 |
| 3 | 2026-08-23 | v5 | 配り方をコンテナバインドのコピーに。書き込みを見出し名に。シートの自動点検 | （このPR） |

---

## 2回目で分かったこと（他リポジトリで先に見るべきもの）

### 1. 「未計測」と書いた画面は、次に必ず出てくる

1回目は `admin.html` を「未計測。同じ書き方が使われている可能性が高い」と書いて終えた。
2回目に測ったら **コントラスト 18件・タップ 73件**。見立ては当たっていた。

**「未計測」は借金である。** 残したら、次の回の最初に返す。

### 2. 直したはずの穴は、状態を変えるまで見えない

1回目は `rt` の色を「色のついた面では継がせる」形に直した。
2回目に測ったら、**タグのラベルだけ `rgba(255,255,255,0.9)` の決め打ちが残っていた**（比 3.52）。
先に書かれていた指定が、あとから足した継承指定より詳しかったためである。

しかも**通常表示では 0件**で、タグを選ぶまで出てこなかった。

```bash
# 横断で数える：色のついた面の rt に色を決め打ちしていないか
grep -rn ":checked[^{]*rt\s*{\|button rt\s*{\|\[class\*=[^{]*rt\s*{" $(git ls-files '*.html' '*.css')
```

### 3. 計測器の穴の分だけ、「0件」は嘘になる

入力欄に打った文字とプレースホルダは、**要素の子ノードにならない。**
文字を数えるだけの計測では丸ごと落ちる。

塞いだところ、**同じ改修前の版が 25件 → 31件**になった。
落ちていた6件のうち3件は、記者ページのプレースホルダ
（「例：運動会で練習の成果を出せたよ」など）で、
**いちばん助けが要る子に向けた文**だった。

計測器には次の3つを必ず入れる。

- `input` / `textarea` / `select` の **値**（`el.value`）
- `::placeholder` の色（`getComputedStyle(el, '::placeholder')`）
- `select` の選択中の文字

### 4. CDN の実行コードは、白い画面にならない壊れ方をする

QR 生成を cdnjs から読んでいた。塞がれると `QRious is not defined` になるが、
**アプリ全体は動く。** `<canvas>` と「LINK」の文字は先に置かれるので、
**先生の画面には枠が見えている。** そのまま印刷すると紙にだけ白い四角が刷られる。

| | 通ったとき | 塞がれたとき |
|---|---:|---:|
| canvas に塗られた画素 | 2500 | **0** |
| 記事一覧・紙面 | 出る | **出る** |

v5 §1 は「実行コードの CDN 依存は、塞がれるとアプリが起動しない（白い画面）」と
書いているが、**起動はするが機能だけが黙って壊れる型**もある。
こちらのほうが見つけにくい。

```bash
# 横断：外部から読んでいる実行コード
grep -rn "<script[^>]*src=[\"']https\?://" $(git ls-files '*.html')
```

### 5. 自己ホスト化は、ライセンスを先に見る

QRious を同梱しようとして調べたら **GPL-3.0** だった。MIT のリポジトリには入れられない。
**CDN から読むだけなら問題にならないが、同梱に切り替えた瞬間に配布物になる。**

MIT の `qr-creator` に差し替えた（17.6KB → 12.0KB）。

```bash
npm view <パッケージ> license   # 同梱を決める前に必ず
```

### 6. ライブラリを差し替えたら、出力そのものを検証する

QR の画素を新旧で比べたら、短い URL は完全一致したが長い URL は 39〜47% 違った。
**版とマスクの選び方が違うだけ**で、どちらも正しい。画素比較は比較にならない。

**復号して元の文字列に戻るかで見た**（jsQR）。そうしたら、素朴な差し替えでは
**GAS のデプロイ URL（73文字）が 50×50 では読めない**ことが分かった。
実画素を 2 倍に持つことで解決した（DPR 補正も同時に満たす）。

**印刷して配る canvas の倍率は、端末の `devicePixelRatio` で決めない。**
先生が DPR 1 の PC で作ると、50×50 しか無い QR が刷られる。

### 7. 品質ゲートは、自分の誤検知を2件出した

わざと壊した入力で確かめるテストを書いたことで、ゲート自身の不具合が出た。

| 見つかった不具合 | 中身 |
|---|---|
| CSS 規則の範囲 | `<style>` の外まで CSS として読み、JavaScript が組み立てる HTML の `style` 属性に反応していた |
| コメント内のタグ | HTML コメントに書いた `<script>` の文字を本物のタグと読み違え、正しいファイルに構文エラーを報告していた |

**「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない。**

### 8. GAS の viewport は3箇所ある

`Code.gs` の `addMetaTag` と、HTML それぞれの `<meta>`。
1回目は `index.html` だけを直しており、**2箇所漏れていた。**

`addMetaTag` はサーバー側の処理なので、HTML を手元で組み立てても再現されない。
どちらが勝つかは本番でしか分からないので、**全部に書けばどれが勝っても正しくなる。**

```bash
grep -rn "addMetaTag\|name=[\"']viewport" $(git ls-files '*.html' '*.gs')
```

---

## 3回目で分かったこと（他リポジトリで先に見るべきもの）

### 9. 「見出し名で読む」と書いてあっても、書き込みは位置決め打ちのことがある

README には以前から「列はヘッダー名で探します。列を入れ替えても動きます」と書いてあった。
`getArticles` はそのとおりだった。**`saveArticle` は 8 要素の配列をそのまま `appendRow` していた。**

先生が列を 1 本入れ替えると、次に届いた記事から本文が記者名の列に入る。
画面には何も出ない。**印刷するまで誰も気づかない。**

```bash
# 横断：読みは名前・書きは位置、という食い違いを探す
grep -rn "appendRow(\[" $(git ls-files '*.gs') | grep -v "header\|HEADER"
grep -rn "indexOf('.*') !== -1 ?" $(git ls-files '*.gs')   # 「無ければ N 列目」
```

### 10. 「見つからなければ既定の位置に」は、親切ではなく事故

`idx.tag = getIdx('Tag') !== -1 ? getIdx('Tag') : 7` という形。
`Tag` の見出しが無く、8 列目に先生のメモ欄があるシートでは、
**メモの中身がタグとして記事一覧に並ぶ。** 無いものは「無い」と扱ったほうが、点検で気づける。

### 11. シートの修整は「足す」だけにする。見出しだけ直すと事故が見えなくなる

見出しの行ごと消えている（＝1 行目がデータになっている）状態で見出しを書き戻すと、
**間違った列に正しいラベルが付く。** そこから先は誰も間違いに気づけない。
`checkSchema_` は「1 行目が見出しに見えない」ときは 1 か所も直さず、人に回す。
GIGA句会プラザ（`code.gs:59`）も同じ結論に達している。**列の中身を動かす判断は人がする。**

### 12. ロックで囲む範囲を間違えると、数人だけが黙って落ちる

`saveArticle` は画像の保存（base64 復号＋ドライブ書き込み。1 件数秒）ごとロックで囲んでいた。
40 人が一斉に押すと直列で 2 分近くになり、児童側の再送 3 回（2+4+6 秒＝最長 22 秒）を追い越す。
**全員が落ちるのではなく、後ろの数人だけが落ちる。** 先生の画面には何も出ない。

囲むのは `appendRow` / `setValue` の 1 回分だけ（v5 §5-3）。
ついでに「ロックを入れ子で取っていないか」をテストで見張るようにした
（偽の `LockService` が、握ったまま `waitLock` されたら例外を投げる）。

### 13. AUDIT の「人間が決めること」は、直したときに消さないと嘘になる

`AUDIT.md` 第 7 節の 1 と 2（編集室に認証が無い／画像が全員に公開）は
**2026-08-08 の PR #3 で塞がれていたのに、記録は「残っている」のままだった。**
その記述が公開中の紹介記事にもそのまま載り、実際より危険なアプリとして 2 週間説明されていた。

```bash
# 横断：AUDIT に「残っている」と書いてある項目が、本当に残っているか
grep -n "人間が決めること" -A 20 AUDIT.md
```

### 14. `package.json` にゲートの名前が無いと、緑のまま本番へ push される

正本 `deploy.yml` は `quality` → `ci` → `check` の順に探し、
どれも無ければ「飛ばします」と表示して**成功として先へ進む**。
このリポジトリは `gas:*` と `deploy` しか持っていなかったので、
**main への push はゲートを 1 つも通らずに GAS へ届いていた。**

### 15. GAS へ送るファイルを減らすと、次のデプロイは「止まる」のが正しい

`index.html` を GAS から外して `post.html` に替えたので、
`gas-deploy.mjs` の `deletions()` が「送ると GAS から消えるファイルがあります」で止まる。
これは正しい動きで、**止まらないほうが危ない**（学校が使っている最中に消えると戻せない）。
1 回だけ `GAS_ALLOW_DELETIONS=1` を付けて流すか、
GAS エディタで `index` を先に消してから流す。

---

## このリポジトリに残っているもの

判断が要るため触っていない。詳細は [`AUDIT.md`](AUDIT.md) 第7節。

- ~~編集室に認証が無い~~ → 2026-08-08 の PR #3 で塞いだ（記録の更新が 2026-08-23 まで漏れていた）
- ~~投稿画像が「リンクを知っている全員が閲覧可」で保存される~~ → 同上。`DOMAIN_WITH_LINK`／`PRIVATE` に変更済み
- 利用者の入力を無害化していない（`=` `+` `-` `@` で始まる文字列をそのままセルへ書く）。
  児童が題名に `=IMPORTXML(...)` と書くと、先生が開いた瞬間に学級の記事データが外部へ流れる。
  手本は `online-publisher-pro` の `safeCellText_` と `reflection_journal` の `csvSafe_`
- `SystemData` シートだけは列を位置で読み書きしている（`Articles` は見出し名に移行済み）
- CSP 未導入（GAS はコードをインラインで持つ構造のため、`include()` への分割が先に要る）
- PWA 未対応（C+型への構成変更が要る）
- `Status` 列が未使用（承認フローが無い）
- モーダルの `role="dialog"` / Esc、`<img>` の `alt`、トーストの `aria-live`、ルビの `<rp>`
- 並び替えのキーボード代替手段
- `pagehide` での確定保存
