{
  "name": "digital-newspaper",
  "version": "2.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "check": "node scripts/check-project.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm test && npm run check && npm run build:sw -- --check",
    "build:sw": "node tools/build-sw.mjs"
  }
}
