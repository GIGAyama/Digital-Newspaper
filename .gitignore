# 秘密情報
.env
.env.*
!.env.example

# note の画面写真を撮り直すときに作る、撮影用の写し
# （docs/note/capture/prepare.mjs が作る。配るものではない）
.capture/

# Node（点検スクリプトを動かしたときに出る）
# package-lock.json は、依存を足したときに版を固定するため意図的に含める
node_modules/
npm-debug.log*

# OS・エディタ
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
